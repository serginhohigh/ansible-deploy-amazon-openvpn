version: '2.4'
services:
  {{ service_name }}:
    container_name: {{ service_name }}
    image: {{ service_name }}
    volumes:
      - "./openvpn:/etc/openvpn"
    ports:
      - "443:443/tcp"
    cap_add:
      - NET_ADMIN
    cpus: '0.5'
    mem_limit: 200M
    pids_limit: 50
    networks:
      - openvpn

networks:
  openvpn:
    name: openvpn
